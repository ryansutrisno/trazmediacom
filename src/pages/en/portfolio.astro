---
import BaseLayout from '@layouts/BaseLayout.astro';
import { getTranslations } from '@i18n/index';
import type { Locale } from '@/types/i18n';
import Navbar from '@components/layout/Navbar.astro';
import Footer from '@components/layout/Footer.astro';
import MobileMenu from '@components/layout/MobileMenu.tsx';
import PortfolioPage from '@components/sections/PortfolioPage.astro';

const locale: Locale = 'en';
const t = getTranslations(locale);
---

<BaseLayout locale={locale} t={t} title={t.portfolio.hero.heading} description={t.portfolio.hero.description}>
  <Navbar locale={locale} t={t} />
  <MobileMenu locale={locale} t={t} client:load />
  <main>
    <PortfolioPage locale={locale} t={t} />
  </main>
  <Footer locale={locale} t={t} />
</BaseLayout>